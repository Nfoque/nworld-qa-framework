.DS_Store

# Ignore .claude/ contents (local settings, transcripts, etc.) EXCEPT
# project-level skills, which we DO version-control so the team shares them.
.claude/*
!.claude/skills/

# Client meeting transcripts and per-client analysis stay local — repo is public.
# Only clients/README.md is committed (workflow documentation).
clients/*
!clients/README.md

# Local Netlify folder
.netlify

sandbox